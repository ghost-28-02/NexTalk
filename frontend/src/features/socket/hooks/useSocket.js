/**
 * useSocket — convenience re-export of the SocketContext consumer.
 * Keeping this as a separate hook file matches the project's hook convention
 * and decouples consumers from the context implementation.
 */
export { useSocket } from '../context/SocketContext';
